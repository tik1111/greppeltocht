on:
  push:
    branches:
      - main
name: Deploy website on push
jobs:
  web-deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - name: Get latest code
      uses: actions/checkout@v3

    - name: Push files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: vserver51.axc.eu/1
        username: git@greppeltocht.scoutingachterhoek.nl
        password: pXxjT7ERuzDeP56KBK8N
        protocol: ftp
        port: 21
